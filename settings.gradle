pluginManagement {
    repositories {
        maven { url 'https://repo.spring.io/milestone' }
        gradlePluginPortal()
        mavenCentral()
    }
}

rootProject.name = 'emails-summarizer'

include 'ui'
include 'api-rs'
